<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".RewardsActivity"
    android:orientation="vertical">

    <!-- User Account Number Text -->
    <TextView
        android:id="@+id/accountNumber"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Account: "
        android:textAlignment="center"
        android:textSize="20dp"
        android:fontFamily="monospace" />

    <!-- Recycler View for Rewards (title, description, image and code) -->
    <android.support.v7.widget.RecyclerView
        android:id="@+id/rewardsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="490dp"
        android:layout_marginTop="20dp"
        android:minHeight="540dp" />

    <!-- Back to MainActivity Button-->
    <Button
        android:id="@+id/backToHomeButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Account Packages"
        android:textColor="@color/blue"
        android:background="#FFFFFF" />

</LinearLayout>